{
  "name": "product-capability-layer",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "workflow:dry-run": "node tools/run_workflow.mjs feature-delivery --dry-run",
    "workflow:help": "node tools/run_workflow.mjs --help"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0"
  }
}
