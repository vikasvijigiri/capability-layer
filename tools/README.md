# Tools

Contains helper scripts for the repo.

- `resolve_capability.py` — small CLI to map user queries to a capability package by scanning `index.md` files.
- `test_resolver.py` — simple test runner that exercises `resolve_capability.py` with sample queries.

Run tests:

```bash
python tools/test_resolver.py
```
