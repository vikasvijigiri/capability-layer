# W3 — Debug / Repair

Notion's shape: Reproduce → collect minimal evidence → localize fault →
fix → targeted test → regression check.

**This is the `debugging` skill, directly** — read `.claude/skills/debugging/SKILL.md`.
Its four phases (Root cause, Pattern, Hypothesis, Fix) already implement
this shape exactly; nothing here restates the procedure.

Entered from anywhere in the chain on a failure — `.claude/workflow.md`'s
Off-chain capabilities table names it as the "Diagnose" owner — or
directly via `/debug`.
