# Manual environment inputs for this repository.
# Copy real values into your own shell/user environment — never commit a
# populated .env with real secrets.

# GitHub PAT used by the `github` MCP server (.mcp.json) to call
# https://api.githubcopilot.com/mcp/. Needs at least `repo` scope.
# Generate at: https://github.com/settings/tokens
GITHUB_TOKEN=
